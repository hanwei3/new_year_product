import api from "../api.js";
import request from "../request.js";

/*
  示例
   function getHome(config) {
      return request.getRequest({
        url: api.api.Home.HomeData,
        data: config
      })
    }
*/ 


export default {
  
}
